<section class="download-guide text-center">
    <div class="container">
      <div class="row js-animate animate-fade-in">
        {% if section.settings.section_title != blank %}
        <h1>{{ section.settings.section_title }}</h1>
        {% endif %}
        {% if section.settings.section_subtitle != blank %}
        <p class="sub-title">{{ section.settings.section_subtitle }}</p>
        {% endif %}
      </div>

      <div class="download-btn js-animate animate-fade-in animated"><a href="{{ 'Omega-3.pdf' | asset_url }}" target="_blank" download><span><img src="{{ 'icon-download.svg' | asset_url }}">{{ section.settings.download_link_txt }}</span></a></div>
    </div>
</section>

<style>
    .learn-more h1 {
        color: #1f3337;
        font-size: 35px;
        line-height: 1.4;
        width: 100%;
        letter-spacing: 0;
    }

    .banner__title-shape:after{
        display: none !important;
    }
    @media (max-width: 768px) {
        .learn-more h1 {
            font-size: 26px !important;
        }
    }
</style>

{% schema %}
    {
        "name": "Download Guide | O3",
        "settings": [
            {
                "type": "text",
                "id": "section_title",
                "label": "Heading",
                "default": "Ready to talk to your health practitioner about Omega-3 and Needed?"
            },
            {
                "type": "textarea",
                "id": "section_subtitle",
                "label": "Subheading"
            },
            {
                "type": "text",
                "id": "download_link_txt",
                "label": "Download link text",
                "default": "Download our Guide"
            }
        ]
    }

{% endschema %}
